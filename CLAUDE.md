# Project Guidelines

## Mermaid Diagrams

- All RGB color values in Mermaid `style` directives must not exceed `0xa0` per channel (i.e., no channel above 160 decimal). For example, use `#4a70a0` not `#4a90d9`.
